export class Plan {

    constructor(
        nombre: string,
        valor: number,
        max_personas: number,
        cuotas: number,
        nit: string,
        formaPago: string,
        valorPersona?: number,
        carencias?: string,
        descripcion?: string
    ) { }

}
